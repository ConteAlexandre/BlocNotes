# Import package os for the paths, created a file for variable for most clarity
import os

# Define different variables for most facility in the project and method
CUR_DIR = os.path.dirname(__file__)
DATA_DIR = os.path.join(CUR_DIR, "data")